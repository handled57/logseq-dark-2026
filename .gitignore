dist/
# The verse text is a licensed edition: it is built locally by
# scripts/build-bible-index.mjs and never committed. The manifest that script
# also writes carries no verse text, and is committed.
resources/*
!resources/bible.books.json
.acceptance-graph/
.DS_Store
npm-debug.log*
